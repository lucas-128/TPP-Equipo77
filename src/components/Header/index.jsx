import { HeaderContainer, HeaderTitle } from "./styled";

export const Header = () => {
  return (
    <HeaderContainer>
      <HeaderTitle>Nombre de la aplicación</HeaderTitle>
    </HeaderContainer>
  );
};
